/*
Desafio 6 - 29/04/2024
Crie um array 2D com duas linhas e dez colunas. A primeira será preenchida com valores aleatórios entre 1 e 100. 
A segunda linha será preenchida com o z-score referente a primeira linha.
Por que o z-score é importante? É útil padronizar os valores brutos de uma distribuição normal convertendo-os 
em z-score porque:
Permite aos pesquisadores calcular a probabilidade de um valor ocorrer dentro de uma distribuição normal padrão;
Permite-nos comparar dois valores de amostras diferentes (que podem ter médias e desvios padrão diferentes).

A fórmula para calcular um z-score é z = (x-μ)/σ, onde x é o valor bruto, μ é a média da população e σ é o desvio 
padrão da população.


A fórmula para cálculo do desvio padrão populacional. Calcule a média dos valores. Para cada valor bruto, subtraia 
a média, e eleve o resultado ao quadrado. Some todos esses resultados e divida pela quantidade de valores. Calcule 
a raiz quadrada dessa soma.





Como mostra a fórmula, o z-score é simplesmente o valor bruto menos a média populacional, dividido pelo desvio padrão 
populacional.
Pode usar a biblioteca <math.h> do C. 
Raiz quadrada: sqrt(valor) → x = sqrt(25); //x = 5
Potência: pow(base, expoente) → y = pow(5, 2); //y = 25

*/

#include <stdio.h>
#include <math.h>

int main() {

    return 0;
}

